import "../global.css";

export const metadata = {};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  const a = 4;
  {
  },
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
